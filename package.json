{
  "name": "ts-ast",
  "version": "0.0.1",
  "description": "typescript ast indexer",
  "main": "build/index.js",
  "scripts": {
    "start": "nodemon build/index.js",
    "dev": "npm run build && concurrently \"npm start\" \"npm run build:watch\"",
    "build": "tsc src/index.ts --outDir build/",
    "build:watch": "tsc src/index.ts --outDir build/ -d -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mateusz przybylowski",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.1.0",
    "typescript": "^3.7.3"
  },
  "devDependencies": {
    "concurrently": "^5.0.1",
    "nodemon": "^2.0.2"
  }
}
